<?xml version="1.0" encoding="UTF-8"?>
<project name="me-media" basedir="../">
    <property file="${basedir}/build/build.properties"/>
    <import file="${base-tasks-directory}/base.build.xml"/>

    <target name="phpunit">
        <echo>@ToDo: Disable phpunit because tests must be refactored!</echo>
    </target>
</project>
